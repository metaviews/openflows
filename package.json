{
  "name": "openflows",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "eleventy",
    "check:links": "node scripts/validate-links.js",
    "cleanup:drafts": "node scripts/cleanup-malformed-drafts.js",
    "audit:practitioner-social": "node scripts/audit-practitioner-social.js",
    "server": "node server/index.js",
    "test": "node --test tests/*.test.js",
    "server:dev": "node --watch server/index.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "discord.js": "^14.26.3",
    "xactions": "^3.1.0"
  }
}
